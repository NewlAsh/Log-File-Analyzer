# Log-File-Analyzer
A C++ log analyzer built using OOP, STL, and file handling that parses log files, counts log levels, identifies the most frequent messages, and generates a detailed summary report.
